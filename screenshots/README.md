# Screenshots

Drop terminal captures here and reference them from the top-level `README.md`.

Suggested set:
- `tui-default.png` - full layout on a 16-core host with NVIDIA GPU
- `tui-compact.png` - `-compact` view
- `tui-multigpu.png` - multi-GPU server
- `prometheus-grafana.png` - sample dashboard

PNG, ~2x retina, dark terminal background, default font. Crop tightly. Keep filenames in kebab-case.
